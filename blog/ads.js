/* ============================================================
   ads.js — retired
   The ad slots this file used to populate (top banner, both
   sidebars, in-content rectangle, native, footer banner — all
   served via Adsterra/highperformanceformat.com) have been removed
   from every blog page. Ads on this site are now served by
   Mediavine, whose own script tag (added to every page's <head>)
   handles ad placement automatically — no per-page loader needed.
   This file is kept as an empty, harmless stub only so that any
   cached/old page still requesting it doesn't get a 404.
   ============================================================ */
